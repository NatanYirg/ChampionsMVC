@model IEnumerable<TheChampions.Models.Registration>


@{
    Layout = "_Layout";
}



@*<section class="search-bar">
    <form asp-controller="camp" method="get" asp-action="Search">
        <input type="text" name="term" placeholder="search here">
        <button type="submit"><i class="fa fa-search"></i></button>
        <a class="btn btn-default" href="/camp/Search">All</a>
    </form>
</section>*@

@*@if (Model.Count > 0)
{
    <table class="table">
        <thead>
        <tr>
            <th>
                    @Html.DisplayNameFor(model => model.FirstName)
            </th>
            <th>Last Name</th>
            <th>Age</th>
            <th>Phone Number</th>
        </tr>
        @foreach (var student in Model)
        {
            <tr>
                <td>@student.FirstName</td>
                <td>@student.LastName</td>
                <td>@student.age</td>
                <td>@student.tel_No</td>
            </tr>
        }
    </table>
}
else
{
    <p>No search results found.</p>
}*@





<div class="row">
    <div class="col-md-8 offset-md-2">

    </div>
</div>
 
<h1>Search Results</h1>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.age)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tel_No)
            </th>
       </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
       <tr>
            <td>
                    @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.age)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.tel_No)
                </td>
            
                <td>
                    <form asp-action="Delete" asp-route-id="@item.id">
                        <div class="btn-group btn-group-sm">
                            <a asp-action="Edit" asp-route-id="@item.id" class="btn btn-warning">
                                <i class="fa-solid fa-pen-to-square"></i>
                            </a>
                            <button type="submit" class="btn btn-danger"
                                    onclick="return confirm('Are you sure you want to delete this record?')">
                                <i class="far fa-trash-alt"></i>
                            </button>
                        </div>
                       </form>
               
            </td>
        </tr>
}
    </tbody>
</table>
